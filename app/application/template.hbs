<nav class="navbar navbar-expand-lg navbar-light bg-light">
  {{link-to 'Customers App' 'index' class='navbar-brand'}}
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <ul class="navbar-nav">
      <li class="nav-item">
        {{link-to 'Home' 'index' class='nav-link'}}
      </li>

      {{#unless session.isAuthenticated}}
        <li class="nav-item">
          {{link-to 'Sign Up' 'users/new' class='nav-link'}}
        </li>
      {{/unless}}

      {{#if session.isAuthenticated}}
        <li class="nav-item">
          {{link-to 'Customers' 'customers' class='nav-link'}}
        </li>

        <li class="nav-item">
          <a {{action 'invalidateSession'}} url="#" class="nav-link invalidate-session">Logout</a>
        </li>
      {{else}}
        <li class="nav-item">
          {{link-to 'Login' 'users/login' class='nav-link'}}
        </li>
      {{/if}}

    </ul>
  </div>
</nav>

<div class="container">
  {{ outlet }}
</div>
